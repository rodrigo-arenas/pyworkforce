import{_ as s,o,c as a,a2 as i}from"./chunks/framework.BUDI7pkh.js";const u=JSON.parse('{"title":"API — Breaks","description":"","frontmatter":{},"headers":[],"relativePath":"api/breaks.md","filePath":"api/breaks.md","lastUpdated":1782489270000}'),t={name:"api/breaks.md"};function d(n,e,r,c,l,h){return o(),a("div",null,[...e[0]||(e[0]=[i(`<h1 id="api-—-breaks" tabindex="-1">API — Breaks <a class="header-anchor" href="#api-—-breaks" aria-label="Permalink to &quot;API — Breaks&quot;">​</a></h1><p><code>pyworkforce.breaks</code></p><h2 id="breakscheduler" tabindex="-1">BreakScheduler <a class="header-anchor" href="#breakscheduler" aria-label="Permalink to &quot;BreakScheduler&quot;">​</a></h2><div class="language-python vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">BreakScheduler(</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    num_days,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    periods,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    shifts_coverage,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    scheduled_resources,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    breaks,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    min_coverage,</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    max_search_time</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">120.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    num_search_workers</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p>Schedules breaks for agent slots within each shift to maintain minimum coverage throughout the planning horizon. Each shift/day combination has <code>scheduled_resources[shift][day]</code> anonymous agent slots; the solver assigns a start period to every break for every slot while enforcing break windows, non-overlap, and coverage constraints.</p><p><strong>Parameters</strong></p><ul><li><strong>num_days</strong> (<code>int</code>) — number of days in the planning horizon.</li><li><strong>periods</strong> (<code>int</code>) — number of periods per day (e.g. <code>48</code> for 30-minute slots in a 24-hour day).</li><li><strong>shifts_coverage</strong> (<code>dict</code>) — <code>{&quot;shift_name&quot;: coverage_array}</code> where <code>coverage_array</code> has length <code>periods</code> and uses <code>1</code> when the shift is active, <code>0</code> otherwise.</li><li><strong>scheduled_resources</strong> (<code>dict</code>) — <code>{&quot;shift_name&quot;: [count_day0, count_day1, …]}</code> with the number of agent slots per shift per day (typically the output of the scheduling step).</li><li><strong>breaks</strong> (<code>list[dict]</code>) — break definitions. Each dict must have: <ul><li><code>name</code> (<code>str</code>) — unique identifier.</li><li><code>duration_periods</code> (<code>int</code>) — break length in periods.</li><li><code>min_start_after</code> (<code>int</code>, default <code>0</code>) — periods into the shift before the break may start.</li><li><code>max_end_before</code> (<code>int</code>, default <code>0</code>) — periods remaining in the shift after the break must finish.</li></ul></li><li><strong>min_coverage</strong> (<code>list</code>) — 2-D array <code>[num_days][periods]</code> — minimum agents that must remain available (not on break) at each period.</li><li><strong>max_search_time</strong> (<code>float</code>, default <code>120.0</code>) — maximum solver wall-clock time in seconds.</li><li><strong>num_search_workers</strong> (<code>int</code>, default <code>2</code>) — number of parallel search workers.</li></ul><p><strong>Attributes</strong></p><ul><li><strong>solution_</strong> (<code>dict</code> or <code>None</code>) — the last result returned by <code>solve()</code>, or <code>None</code> if <code>solve()</code> has not been called.</li></ul><p><strong>Methods</strong></p><ul><li><p><code>solve()</code> → <code>dict</code></p><p>Run the CP-SAT solver and return a result dict with:</p><ul><li><code>status</code> (<code>str</code>) — <code>&quot;OPTIMAL&quot;</code>, <code>&quot;FEASIBLE&quot;</code>, or <code>&quot;INFEASIBLE&quot;</code>.</li><li><code>cost</code> (<code>float</code>) — objective value (<code>0.0</code> for optimal/feasible; <code>-1</code> when infeasible).</li><li><code>break_schedule</code> (<code>list[dict]</code>) — one entry per <code>(shift, day, slot, break)</code>. Each entry contains <code>shift</code>, <code>day</code>, <code>slot</code>, <code>break_name</code>, <code>start_period</code> (inclusive) and <code>end_period</code> (exclusive).</li></ul></li><li><p><code>get_params()</code> → <code>dict</code></p></li></ul><p>See the <a href="/pyworkforce/stable/guide/breaks">Break scheduling guide</a>.</p>`,12)])])}const k=s(t,[["render",d]]);export{u as __pageData,k as default};
