import{_ as a,o as i,c as s,a2 as t}from"./chunks/framework.BUDI7pkh.js";const g=JSON.parse('{"title":"API — Queuing","description":"","frontmatter":{},"headers":[],"relativePath":"api/queuing.md","filePath":"api/queuing.md","lastUpdated":1782489270000}'),o={name:"api/queuing.md"};function l(n,e,r,d,c,p){return i(),s("div",null,[...e[0]||(e[0]=[t(`<h1 id="api-—-queuing" tabindex="-1">API — Queuing <a class="header-anchor" href="#api-—-queuing" aria-label="Permalink to &quot;API — Queuing&quot;">​</a></h1><p><code>pyworkforce.queuing</code></p><h2 id="erlangc" tabindex="-1">ErlangC <a class="header-anchor" href="#erlangc" aria-label="Permalink to &quot;ErlangC&quot;">​</a></h2><div class="language-python vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">ErlangC(transactions, aht, asa, interval, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">shrinkage</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p>Erlang C (M/M/c) queue: Poisson arrivals, exponential handling times, infinite queue, no abandonment.</p><p><strong>Parameters</strong></p><ul><li><strong>transactions</strong> (<code>float</code>) — transactions arriving in the interval.</li><li><strong>aht</strong> (<code>float</code>) — average handle time.</li><li><strong>asa</strong> (<code>float</code>) — target average speed of answer.</li><li><strong>interval</strong> (<code>float</code>) — interval length.</li><li><strong>shrinkage</strong> (<code>float</code>, default <code>0.0</code>) — unavailable fraction, in <code>[0, 1)</code>.</li></ul><p><strong>Attributes</strong></p><ul><li><strong>intensity</strong> (<code>float</code>) — offered load in Erlangs.</li></ul><p><strong>Methods</strong></p><ul><li><code>required_positions(service_level, max_occupancy=1.0)</code> → <code>dict</code> with <code>raw_positions</code>, <code>positions</code>, <code>service_level</code>, <code>occupancy</code>, <code>waiting_probability</code>.</li><li><code>waiting_probability(positions, scale_positions=False)</code> → <code>float</code></li><li><code>service_level(positions, scale_positions=False)</code> → <code>float</code></li><li><code>achieved_occupancy(positions, scale_positions=False)</code> → <code>float</code></li><li><code>get_params()</code> → <code>dict</code></li></ul><p>Set <code>scale_positions=True</code> when <code>positions</code> already includes shrinkage.</p><p>See the <a href="/pyworkforce/stable/guide/erlangc">Erlang C guide</a>.</p><h2 id="erlanga" tabindex="-1">ErlangA <a class="header-anchor" href="#erlanga" aria-label="Permalink to &quot;ErlangA&quot;">​</a></h2><div class="language-python vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">ErlangA(transactions, aht, asa, interval, patience, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">shrinkage</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p>Erlang A (M/M/c+M) queue with customer abandonment. All metrics are computed exactly from the birth-death stationary distribution.</p><p><strong>Parameters</strong> — as <code>ErlangC</code>, plus:</p><ul><li><strong>patience</strong> (<code>float</code>) — mean time before a waiting customer abandons.</li></ul><p><strong>Methods</strong></p><ul><li><code>required_positions(service_level, max_occupancy=1.0, max_abandonment=1.0, asa=None)</code> → <code>dict</code> with <code>raw_positions</code>, <code>positions</code>, <code>service_level</code>, <code>occupancy</code>, <code>abandonment_probability</code>, <code>waiting_probability</code>, <code>average_speed_of_answer</code>.</li><li><code>waiting_probability(positions)</code> → <code>float</code></li><li><code>abandonment_probability(positions)</code> → <code>float</code></li><li><code>achieved_occupancy(positions)</code> → <code>float</code></li><li><code>average_speed_of_answer(positions)</code> → <code>float</code></li><li><code>average_queue_length(positions)</code> → <code>float</code></li><li><code>service_level(positions, asa=None)</code> → <code>float</code></li><li><code>get_params()</code> → <code>dict</code></li></ul><p>See the <a href="/pyworkforce/stable/guide/erlanga">Erlang A guide</a>.</p><h2 id="multierlangc" tabindex="-1">MultiErlangC <a class="header-anchor" href="#multierlangc" aria-label="Permalink to &quot;MultiErlangC&quot;">​</a></h2><div class="language-python vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">MultiErlangC(param_grid, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">n_jobs</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">pre_dispatch</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;2 * n_jobs&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p>Evaluate <code>ErlangC</code> over a grid of parameters in parallel.</p><p><strong>Parameters</strong></p><ul><li><strong>param_grid</strong> (<code>dict</code>) — <code>ErlangC</code> constructor arguments, each mapped to a list of values.</li><li><strong>n_jobs</strong> (<code>int</code>, default <code>2</code>) — parallel workers (<code>-1</code> = all CPUs).</li><li><strong>pre_dispatch</strong> (<code>str</code>, default <code>&#39;2 * n_jobs&#39;</code>) — joblib pre-dispatch.</li></ul><p><strong>Methods</strong> (each takes an <code>arguments_grid</code> dict and returns a list of results)</p><ul><li><code>required_positions(arguments_grid)</code></li><li><code>service_level(arguments_grid)</code></li><li><code>waiting_probability(arguments_grid)</code></li><li><code>achieved_occupancy(arguments_grid)</code></li></ul><p><strong>Attributes</strong> (set after the matching method runs, aligned with results)</p><ul><li><code>required_positions_params</code>, <code>service_level_params</code>, <code>waiting_probability_params</code>, <code>achieved_occupancy_params</code> — lists of <code>(erlang_params, method_params)</code> tuples.</li></ul><p>See the <a href="/pyworkforce/stable/guide/multierlang">MultiErlangC guide</a>.</p><h2 id="multierlanga" tabindex="-1">MultiErlangA <a class="header-anchor" href="#multierlanga" aria-label="Permalink to &quot;MultiErlangA&quot;">​</a></h2><div class="language-python vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">MultiErlangA(param_grid, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">n_jobs</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">pre_dispatch</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;2 * n_jobs&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p>The abandonment-aware counterpart of <code>MultiErlangC</code>: evaluates <code>ErlangA</code> over a parameter grid in parallel. <code>param_grid</code> takes <code>ErlangA</code> constructor arguments (including <code>patience</code>).</p><p><strong>Methods</strong> (each takes an <code>arguments_grid</code> dict and returns a list of results)</p><ul><li><code>required_positions(arguments_grid)</code></li><li><code>service_level(arguments_grid)</code></li><li><code>waiting_probability(arguments_grid)</code></li><li><code>abandonment_probability(arguments_grid)</code></li><li><code>achieved_occupancy(arguments_grid)</code></li><li><code>average_speed_of_answer(arguments_grid)</code></li><li><code>average_queue_length(arguments_grid)</code></li></ul><p>Each method stores the matching <code>(erlang_params, method_params)</code> tuples in a <code>&lt;method&gt;_params</code> attribute, aligned with the results.</p><p>See the <a href="/pyworkforce/stable/guide/scenarios">scenarios tutorial</a>.</p><h2 id="erlangb" tabindex="-1">ErlangB <a class="header-anchor" href="#erlangb" aria-label="Permalink to &quot;ErlangB&quot;">​</a></h2><div class="language-python vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">ErlangB(transactions, aht, interval, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">shrinkage</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p>Erlang B (M/M/c/c) pure-loss queue: Poisson arrivals, exponential holding times, <strong>no waiting room</strong> — a call that finds all channels busy is blocked and lost. The blocking probability is computed via the numerically stable Erlang B recursion.</p><p><strong>Parameters</strong></p><ul><li><strong>transactions</strong> (<code>float</code>) — calls arriving in the interval.</li><li><strong>aht</strong> (<code>float</code>) — average holding time (same unit as <code>interval</code>).</li><li><strong>interval</strong> (<code>float</code>) — interval length.</li><li><strong>shrinkage</strong> (<code>float</code>, default <code>0.0</code>) — unavailable fraction, in <code>[0, 1)</code>.</li></ul><p><strong>Attributes</strong></p><ul><li><strong>intensity</strong> (<code>float</code>) — offered traffic in Erlangs.</li></ul><p><strong>Methods</strong></p><ul><li><code>required_positions(max_blocking, max_occupancy=1.0)</code> → <code>dict</code> with <code>raw_positions</code>, <code>positions</code>, <code>blocking_probability</code>, <code>occupancy</code>.</li><li><code>blocking_probability(positions, scale_positions=False)</code> → <code>float</code></li><li><code>achieved_occupancy(positions, scale_positions=False)</code> → <code>float</code></li><li><code>get_params()</code> → <code>dict</code></li></ul><p>Set <code>scale_positions=True</code> when <code>positions</code> already includes shrinkage.</p><p>See the <a href="/pyworkforce/stable/guide/erlangb">Erlang B guide</a>.</p><h2 id="multierlangb" tabindex="-1">MultiErlangB <a class="header-anchor" href="#multierlangb" aria-label="Permalink to &quot;MultiErlangB&quot;">​</a></h2><div class="language-python vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">MultiErlangB(param_grid, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">n_jobs</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">pre_dispatch</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;2 * n_jobs&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p>Evaluate <code>ErlangB</code> over a grid of parameters in parallel. Interface mirrors <code>MultiErlangC</code>.</p><p><strong>Parameters</strong></p><ul><li><strong>param_grid</strong> (<code>dict</code>) — <code>ErlangB</code> constructor arguments, each mapped to a list of values.</li><li><strong>n_jobs</strong> (<code>int</code>, default <code>2</code>) — parallel workers (<code>-1</code> = all CPUs).</li><li><strong>pre_dispatch</strong> (<code>str</code>, default <code>&#39;2 * n_jobs&#39;</code>) — joblib pre-dispatch.</li></ul><p><strong>Methods</strong> (each takes an <code>arguments_grid</code> dict and returns a list of results)</p><ul><li><code>required_positions(arguments_grid)</code></li><li><code>blocking_probability(arguments_grid)</code></li><li><code>achieved_occupancy(arguments_grid)</code></li></ul><p><strong>Attributes</strong> (set after the matching method runs, aligned with results)</p><ul><li><code>required_positions_params</code>, <code>blocking_probability_params</code>, <code>achieved_occupancy_params</code> — lists of <code>(erlang_params, method_params)</code> tuples.</li></ul><p>See the <a href="/pyworkforce/stable/guide/erlangb">Erlang B guide</a>.</p><h2 id="results-to-dataframe" tabindex="-1">results_to_dataframe <a class="header-anchor" href="#results-to-dataframe" aria-label="Permalink to &quot;results_to_dataframe&quot;">​</a></h2><div class="language-python vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> pyworkforce.utils </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> results_to_dataframe</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">results_to_dataframe(results, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">params</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">None</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p>Combine a <code>Multi*</code> result list (and the matching <code>*_params</code>) into a tidy <code>pandas.DataFrame</code>, one row per scenario. Result keys that collide with an input parameter name are suffixed with <code>_result</code> so both the target and the achieved value are kept.</p>`,62)])])}const u=a(o,[["render",l]]);export{g as __pageData,u as default};
